package Real;

public class Maps {
    //TOPIC30
    //used for Pairs(Key and Value).KEy must be unique
    //does not support primitives. Wrapper class only
    //HashMap: Fastest, accepts null key & value, random order
    //LinkedHasMap: maintains the insertion order, accepts null key and value
    //xml
}
