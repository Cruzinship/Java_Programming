package Real;

public class FinalVsFinallyVsFinalize {
//Topic26

    //Final is a keyword put onto variables or methods(Not Constructors)
    //Final makes any variable and method immutable

    //Finally is only usable for try catch
    //And this is a method only used for executing something within a try catch statement

    //Finalize is a method(Garbage Collection handles this for us)
    //Finalize is used to release all resources used by object before it is deleted by GC
}
