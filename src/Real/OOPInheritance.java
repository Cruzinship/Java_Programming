package Real;

public class OOPInheritance{
    //OOP Inheritance: building parent & child relations between classes
    //extends
    //implements
    //classA extends classB
    //interface extends interfaceB, interfaceC..
    //ClassA implements interfaceB
    //ClassA extends classB implements interfaceC
    //subclass(child); can inherit all the visible variable & methods ONLY
    //superclass(parent); does not inherit anything

    /*
    BasePage:
    public BasePage(){
    PageFactory.initElement(driver, this)
    }


    @FindBy

    HomePage extends BasePage

    SecondPage extends BasePage

    TestBase:
    @BeforeMethod{
    Open Browser
    implicit wait
    Maximize
    navigate
    }
    @AfterMethod{
    close the browser

    takesScreenshot and addIt to the report if failed
    }

    TestCase1 extends TestBase:

    @Test{
    Open Browser
    implicit wait
    Maximize
    navigate
    }
    @Test{
    Open Browser
    implicit wait
    Maximize
    navigate
    }
    @Test{
    Open Browser
    implicit wait
    Maximize
    navigate
    }
*/
}
